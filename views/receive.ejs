<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Venra Bank | Receive Money</title>
  <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
  <style>
    :root { --primary: #4f46e5; --bg-color: #f8fafc; --surface: #ffffff; --text-main: #0f172a; --text-muted: #64748b; --border: #e2e8f0; --btc: #F7931A; --eth: #627EEA; --danger: #ef4444; --font: 'Inter', sans-serif; }
    * { box-sizing: border-box; margin: 0; padding: 0; font-family: var(--font); }
    body { background-color: var(--bg-color); color: var(--text-main); display: flex; height: 100vh; overflow: hidden; }

    /* DARK SIDEBAR STYLES */
    .mobile-header { display: none; padding: 16px 20px; background: #0f172a; color: white; align-items: center; justify-content: space-between; }
    .mobile-header img { height: 35px; object-fit: contain; }
    .menu-btn { background: none; border: none; font-size: 24px; cursor: pointer; color: white; }
    .sidebar-overlay { display: none; position: fixed; inset: 0; background: rgba(15, 23, 42, 0.6); z-index: 99; backdrop-filter: blur(2px); }

    .sidebar { width: 260px; background: linear-gradient(135deg, #0f172a 0%, #1e293b 100%); color: white; display: flex; flex-direction: column; padding: 24px 20px; z-index: 100; box-shadow: 4px 0 15px rgba(0,0,0,0.05); }
    .brand { display: flex; align-items: center; justify-content: center; margin-bottom: 40px; padding: 0 10px; }
    .brand img { height: 75px; object-fit: contain; } 
    
    .nav-menu { display: flex; flex-direction: column; gap: 8px; flex: 1; }
    .nav-link { display: flex; align-items: center; gap: 12px; padding: 12px 16px; border-radius: 12px; color: #94a3b8; text-decoration: none; font-weight: 600; font-size: 14px; transition: all 0.2s; }
    .nav-link:hover { background: rgba(255,255,255,0.05); color: white; }
    .nav-link.active { background: var(--primary); color: white; box-shadow: 0 4px 12px rgba(79, 70, 229, 0.3); }
    
    .user-profile { display: flex; align-items: center; gap: 12px; padding: 12px; background: rgba(0,0,0,0.2); border-radius: 12px; margin-bottom: 16px; border: 1px solid rgba(255,255,255,0.05); }
    .avatar { width: 36px; height: 36px; background: linear-gradient(135deg, var(--primary), #818cf8); color: white; border-radius: 50%; display: flex; align-items: center; justify-content: center; font-weight: 700; font-size: 14px; }
    .user-info { display: flex; flex-direction: column; }
    .user-info span { font-size: 13px; font-weight: 700; color: white; }
    .user-info small { font-size: 11px; color: #94a3b8; }

    /* PAGE SPECIFIC STYLES */
    .main-content { flex: 1; overflow-y: auto; padding: 40px 60px; }
    .topbar { margin-bottom: 30px; }
    .topbar h1 { font-size: 28px; font-weight: 800; letter-spacing: -1px; }

    .receive-grid { display: grid; grid-template-columns: 1fr 1fr; gap: 24px; max-width: 1000px; align-items: start; }
    .card { background: var(--surface); border-radius: 24px; padding: 32px; border: 1px solid var(--border); box-shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.05); }
    .fiat-card { border-top: 4px solid var(--primary); }
    
    .detail-row { display: flex; justify-content: space-between; align-items: center; padding: 16px 0; border-bottom: 1px solid var(--border); }
    .detail-row:last-child { border-bottom: none; padding-bottom: 0; }
    .detail-label { font-size: 13px; color: var(--text-muted); font-weight: 600; text-transform: uppercase; letter-spacing: 0.5px; }
    .detail-value { font-size: 15px; font-weight: 700; font-family: monospace; letter-spacing: 0.5px; word-break: break-all; color: var(--text-main); margin-top: 4px;}

    .pending-alert { background: #fefce8; color: #854d0e; padding: 16px; border-radius: 12px; margin-bottom: 24px; text-align: center; font-size: 14px; font-weight: 600; border: 1px solid #fde047; }

    @media (max-width: 900px) {
      body { flex-direction: column; }
      .mobile-header { display: flex; }
      .sidebar { position: fixed; left: -100%; top: 0; bottom: 0; transition: left 0.3s ease; width: 280px; }
      .sidebar.open { left: 0; }
      .sidebar-overlay.open { display: block; }
      .main-content { padding: 20px; flex: 1; }
      .receive-grid { grid-template-columns: 1fr; }
      .card { padding: 24px; }
    }
  </style>
</head>
<body>

  <div class="mobile-header">
    <div style="display: flex; align-items: center; gap: 10px;">
      <img src="/logo.png" alt="Venra Bank">
    </div>
    <button class="menu-btn" onclick="toggleSidebar()">☰</button>
  </div>
  <div class="sidebar-overlay" onclick="toggleSidebar()"></div>

  <aside class="sidebar">
    <div class="brand">
      <img src="/logo.png" alt="Venra Bank">
    </div>
    <nav class="nav-menu">
      <a href="/dashboard" class="nav-link">Home</a>
      <a href="/send" class="nav-link">Send Money</a>
      <a href="/receive" class="nav-link active">Receive Money</a>
      <a href="/transfers" class="nav-link">Transactions</a>
      <a href="/hisa" class="nav-link">Savings Account</a>
      <a href="/account" class="nav-link">Account Details</a>
      <a href="/support" class="nav-link">Support Hub</a>
      <a href="/profile" class="nav-link">Settings</a>
    </nav>
    
    <div style="margin-top: auto;">
      <div class="user-profile">
        <div class="avatar"><%= username.charAt(0).toUpperCase() %></div>
        <div class="user-info"><span>@<%= username %></span><small><%= account.tier %> Plan</small></div>
      </div>
      <form action="/logout" method="POST">
        <button type="submit" class="nav-link" style="width: 100%; background: transparent; border: none; cursor: pointer; color: #f87171; font-family: var(--font);">
          <svg viewBox="0 0 24 24" width="18" height="18" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" style="flex-shrink: 0; margin-right: 12px;"><path d="M9 21H5a2 2 0 0 1-2-2V5a2 2 0 0 1 2-2h4"></path><polyline points="16 17 21 12 16 7"></polyline><line x1="21" y1="12" x2="9" y2="12"></line></svg>
          Log Out
        </button>
      </form>
    </div>
  </aside>

  <main class="main-content">
    <div class="topbar">
      <h1>Receive Funds</h1>
      <p style="color: var(--text-muted); margin-top: 8px;">Share these details with the sender to receive money securely.</p>
    </div>

    <% if (userStatus === 'PENDING') { %>
      <div class="pending-alert">
        ⏳ Your account is currently pending verification. Your full routing details will be generated once your account is active. Please contact support.
      </div>
    <% } %>

    <div class="receive-grid">
      <div class="card fiat-card">
        <h3 style="margin-bottom: 24px; color: var(--primary);">Fiat (EUR) Routing</h3>
        
        <div class="detail-row" style="flex-direction: column; align-items: flex-start; gap: 4px; <%= userStatus === 'PENDING' ? 'border: none;' : '' %>">
          <div class="detail-label">Beneficiary Name</div>
          <div class="detail-value" style="font-family: var(--font);"><%= account.name %></div>
        </div>

        <% if (userStatus !== 'PENDING') { %>
          <div class="detail-row" style="flex-direction: column; align-items: flex-start; gap: 4px;">
            <div class="detail-label">IBAN</div>
            <div class="detail-value"><%= account.iban %></div>
          </div>
          <div class="detail-row" style="flex-direction: column; align-items: flex-start; gap: 4px; border: none;">
            <div class="detail-label">BIC / SWIFT</div>
            <div class="detail-value"><%= account.swift %></div>
          </div>
        <% } %>
      </div>

      <% if (userStatus !== 'PENDING') { %>
        <% if (account.btc_status === 'ACTIVE') { %>
        <div class="card" style="border-top: 4px solid var(--btc);">
          <h3 style="margin-bottom: 24px; color: var(--btc);">Bitcoin (BTC)</h3>
          <div class="detail-row" style="flex-direction: column; align-items: flex-start; gap: 8px; border: none;">
            <div class="detail-label">Wallet Address</div>
            <div class="detail-value" style="background: #fefce8; padding: 16px; border-radius: 8px; border: 1px dashed #fde047; width: 100%; text-align: center;"><%= account.btc_address %></div>
          </div>
        </div>
        <% } %>

        <% if (account.eth_status === 'ACTIVE') { %>
        <div class="card" style="border-top: 4px solid var(--eth);">
          <h3 style="margin-bottom: 24px; color: var(--eth);">Ethereum (ETH)</h3>
          <div class="detail-row" style="flex-direction: column; align-items: flex-start; gap: 8px; border: none;">
            <div class="detail-label">Wallet Address</div>
            <div class="detail-value" style="background: #eff6ff; padding: 16px; border-radius: 8px; border: 1px dashed #bfdbfe; width: 100%; text-align: center;"><%= account.eth_address %></div>
          </div>
        </div>
        <% } %>
      <% } %>
    </div>
  </main>

  <script>
    function toggleSidebar() {
      document.querySelector('.sidebar').classList.toggle('open');
      document.querySelector('.sidebar-overlay').classList.toggle('open');
    }
  </script>
</body>
</html>